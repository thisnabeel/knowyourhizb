<%= form_for(@fiqh_case) do |f| %>
  <% if @fiqh_case.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fiqh_case.errors.count, "error") %> prohibited this fiqh_case from being saved:</h2>

      <ul>
      <% @fiqh_case.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :position %><br>
    <%= f.number_field :position %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :fiqh_case_id %><br>
    <%= f.text_field :fiqh_case_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
